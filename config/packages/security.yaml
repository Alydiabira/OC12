security:
    password_hashers:
        App\Entity\User:
            algorithm: auto

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        public:
            pattern: ^/(auth|user|register)
            security: false

        api:
            pattern: ^/   # OK, mais il doit venir APRES le firewall public
            stateless: true
            provider: app_user_provider
            jwt: ~

    access_control:
        - { path: ^/auth, roles: PUBLIC_ACCESS }
        - { path: ^/user$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/register, roles: PUBLIC_ACCESS }
        - { path: ^/conseil, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/meteo, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/admin, roles: ROLE_ADMIN }
